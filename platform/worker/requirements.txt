# =============================================================================
# Worker Service Dependencies
# =============================================================================

# ── Database ────────────────────────────────────────────────────────────────
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# ── Redis ───────────────────────────────────────────────────────────────────
redis==5.0.1

# ── Pydantic / Settings ─────────────────────────────────────────────────────
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv==1.0.0

# ── SupportRAG / RAG Stack ──────────────────────────────────────────────────
# LangChain
langchain>=0.1.16
langchain-community>=0.0.34
langchain-core>=0.1.42

# LLM provider (Groq — fast open-source LLM inference)
groq>=0.4.2

# Embeddings & Vector DB
sentence-transformers>=2.7.0
tf-keras                       # required when Keras 3 is installed (Transformers compat)
faiss-cpu>=1.7.4
numpy>=1.23.0
torch                          # required by sentence-transformers

# HuggingFace datasets (for loading FAQ data on first build)
datasets>=2.18.0
huggingface-hub>=0.21.0

# Data processing
pandas>=2.1.4

# Logging (used by SupportRAG internals)
loguru>=0.7.2

# Async file I/O
aiofiles>=23.2.1
aiohttp>=3.9.1
